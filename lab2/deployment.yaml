apiVersion: apps/v1
kind: Deployment
metadata:
  name: lobster-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lobster-web
  template:
    metadata:
      labels:
        app: lobster-web
    spec:
      containers:
        - name: nginx
          image: nginx:stable
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          configMap:
            name: web-content