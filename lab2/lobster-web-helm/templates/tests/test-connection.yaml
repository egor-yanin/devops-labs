apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lobster-web-helm.fullname" . }}-test-connection"
  labels:
    {{- include "lobster-web-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lobster-web-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
