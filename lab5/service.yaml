apiVersion: v1
kind: Service

metadata:
  name: load-service
  labels:
    app: load-service

spec:
  selector:
    app: load-service

  ports:
    - name: http
      port: 80
      targetPort: 5000

  type: ClusterIP